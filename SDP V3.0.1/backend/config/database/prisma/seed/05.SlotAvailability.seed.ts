// Slot availability is now date-based. Admin configures slots per calendar date via the Slot Hours page.
// No default seed needed.
const seed = async () => {
  console.log("Slot availability: date-based (no default seed)");
};

export default seed;
